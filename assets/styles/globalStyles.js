// assets/styles/globalStyles.js

import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
  gameBackButton: {
    position: 'absolute',
    top: 50,
    left: 10,
    zIndex: 1,
  },
});

export default globalStyles;
