// component/GameControls/style.js

import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  controls: {
    textAlign: 'center',
    fontSize: 30,
    fontWeight: 'bold',
    marginTop: 80,
    marginBottom: 20,
  },
});

export default styles;